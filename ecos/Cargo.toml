[package]
name    = "ecos"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
lazy_static = {version = "1.5.0", features = ["spin_no_std"]}
spin        = "0.9.8"
exo         = { path = "../exo" }